import React, {createRef, useRef, useState} from 'react';
import {StyleSheet} from 'react-native';
import Scann from './src/views/scann';

export default function App() {
  return <Scann />;
}

const styles = StyleSheet.create({});
